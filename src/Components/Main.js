import React, { Fragment } from "react";
import Header from "../Components/Header";

const Main = () => {
  return (
    <Fragment>
      <Header></Header>
    </Fragment>
  );
};

export default Main;
